# Method — Inspection KPI

mAP@0.5 via greedy IoU matching and all-point interpolated AP, then averaged across defect types. That number is for the vendor.

The aisle reads the ledger: two detected fails at one station in a shift freezes the all-clear and resets the rate. PPM with vs without the rule, Pareto, false stops per month. fill-03 style repeat fail is the stop, not the mAP.
