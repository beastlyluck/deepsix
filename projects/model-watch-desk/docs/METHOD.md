# Method — Model watch

Three champions. PSI per feature vs a reference window. Rolling AUC with next-day labels. Traffic light: PSI > 0.2 red, > 0.1 amber; AUC drop > 0.05 also red.

credit_limit drifts from day 8. churn_watch is performance-only from day 10 (no covariate shift). False pages = red days with no injected cause. Detection delay is the product.
